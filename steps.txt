Step 1: Setup environment
python -m venv .venv
.\.venv\Scripts\activate
pip install -r requirements.txt

Step 2: Run and verify API
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
Visit: http://localhost:8000/docs

Step 3: Manual API test
POST /analyze with a resume file
optional job_skills=python,aws,sql
confirm JSON output has:
profile
skills
feedback

Step 4: Add improvements iteratively
a) refine extraction sections (education, experience, projects)
b) add “experience years” calculator
c) add “missing skill recommendations”
d) add “readability + keyword density”